# This directory will contain audio assets for the applet
# Files installed here during development will be found via:
# - COSMIC_APPLET_TIMEPLUS_DATA env var (just run)
# - /usr/share/cosmic-applet-timeplus/sounds/ (system install)
# - ~/.local/share/cosmic-applet-timeplus/sounds/ (user install)
